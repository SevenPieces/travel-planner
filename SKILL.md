---
name: travel-planner
description: 规划家庭或个人旅行攻略：按行程简报收集要素，并行调研核实最新信息（来源链接+"需出发前核实"标注），生成结构化攻略（速览/总览表/每日行程/景点四级优缺点/住宿/交通/预算/打包/Tips/来源）。用于"做旅行攻略、行程规划、帮我规划去某地"类请求。Use when asked to plan a trip, itinerary, or travel guide (Chinese guide output with English filenames) for family or personal travel.
whenToUse: 任何需要产出结构化旅行攻略的请求，尤其带老人、儿童的家庭出行，或要求信息真实可靠、优缺点如实的规划任务。
---

# Travel Planner（旅行攻略生成）

这是一套可复用的旅行规划流程。用法：用户提出"做攻略/规划行程"时，按第 1 节收集输入，按第 2 节并行调研，按第 3 节输出契约撰写，按第 4 节质量门自查，按第 5 节套用复用旋钮。本技能目录 `examples/` 下有完整执行实例（示例行程：西双版纳 6 天 5 晚家庭游），撰写前先读它校准格式与详略。

## 1. 收集输入（行程简报）

先向用户确认（或从对话中提取）以下字段，缺失的标为待确认并优先追问：

| 字段 | 说明 | 示例 |
|---|---|---|
| 目的地/出发地 | 城市 + 区域范围 | 西双版纳（景洪/勐海/勐腊），出发城市（示例） |
| 日期 | 起止日 + 出发/返程时刻 | 第 1 天晚出发，第 6 天下午/晚返程 |
| 人数与成员画像 | 每人：年龄、体力（如"登山只能低强度"）、兴趣、忌口 | 2 位 60+ 长者（其中一位仅低强度）、2 位成人、1 名学龄儿童 |
| 交通 | 大交通（航班/高铁）+ 当地交通（租车/包车） | 直飞 + 租车自驾 |
| 住宿 | 类型偏好 + 每晚预算 + 间数 | 品质酒店优先，200–400 元/间 × 2 间 |
| 行程原则 | 节奏、深度取向 | 从容轻松、深入风土人情、老幼兼顾 |
| 额外/特殊要求 | 任何影响行程的附加条件：忌口、健康限制、纪念日、摄影/购物需求、必去心愿单、预算上限、想避免的项目 | 长者登山仅低强度；行程中某晚安排生日庆祝；不吃生冷；不安排动物表演类项目 |

动笔前先算清楚：实际可用游玩天数（首尾天通常是抵达休整/返程收尾日）、季节特征（雨季/旺季/淡季/极端天气）、每位成员的体力限制落在哪些景点上，以及每条额外/特殊要求的落点（在行程中体现，或明确说明为何无法满足并给出替代）。

## 2. 调研协议（四轨并行）

用子代理并行跑 4 条轨道。每条轨道统一要求：**信息以近两年为准、关键事实附来源链接、冲突或存疑处标注"需出发前核实"、优缺点如实（含争议与负面评价，不因名气大回避）**。

- **轨道A 核心景点组①**：目的地最知名的 4–5 个景点。核实：位置车程、门票（成人/儿童/老人优惠 + 必要附加消费）、开放时间、建议时长、优点、缺点/坑（商业化/人流/体力/季节影响/性价比/争议）、体力强度分级、老人儿童适配、备选地点。
- **轨道B 核心景点组②**：其余重要景点，要求同上，特别如实呈现争议（动物福利、宗教敏感、边境管控、环保争议等）。
- **轨道C 在地深度体验**：村寨、非遗、徒步、早市、特色体验。核实：真实性（是否过度商业化包装）、车程、费用、场次/预约、体力、季节影响、匹配分级。
- **轨道D 后勤**：大交通（班次/价格/旺季判断）、当地交通（租车车型/价格/儿童座椅/保险）、天气气候（季节规律）、住宿（具体酒店/民宿 + 价格 + 利弊）、证件要求、医疗资源、预算参考区间。

同时自查官方风险提示（汛期/地灾/限流/治安公告），纳入攻略的"需核实清单"与 Tips。

## 3. 输出契约（固定结构）

产出一份 Markdown 攻略文件，章节顺序可微调，但内容必须齐全。**语言与命名约定**：攻略正文一律中文（用户是中文用户）；文件与目录名用英文 kebab-case（系统环境为英文）——输出到 `<cwd>/travel-guides/`，文件名 `<目的地英文>-<出发日期>-<可选标签>.md`（如 `bali-2099-01-01-family-trip.md`）：

0. **速览**：一句话行程 + 核心决策表（需要用户拍板的 3–5 件事）
1. **需出发前核实清单**：逐项列出时效存疑信息
2. **行程总览表**：日期/星期/白天/晚间/夜宿；总览后附**路线图（目的地内每日动线）**——文字链式总览 + 分日路段表（路段/交通/距离/车程/对应安排）+ ````map` 图块（HTML 渲染为真实地图：总览图 + 每日各一张，Leaflet + 高德瓦片，需联网；无坐标时退化为 ````route` 节点示意）；路线图只含目的地内动线，不含出发地⇄机场航班段；**总览图放在行程总览/路线图处，每日图放在对应日期的小节内**
3. **每日详细行程**：时间轴表 + 用餐 + 备选方案 + 雨天/坏天气预案
4. **景点库**：按"经典必去 / 值得一去 / 可去可不去 / 本次不安排"四级分类；每个景点给位置车程、费用、开放时间、优点、缺点、体力分级、老人儿童适配、备选地点
5. **住宿方案**：区域对比 + 推荐方案（含搬酒店的代价）+ 订房提醒
6. **交通与自驾**：航班/租车/路况/证件
7. **预算估算表**：分项 + 合计 + 弹性项说明
8. **打包清单**：证件/衣物/防晒防蚊/药品/电子（按成员画像定制）
9. **Tips**：天气穿着/饮食/民俗礼仪/避坑清单/老幼专项/安全应急
10. **信息来源**：主要链接汇总
11. **测评视频与实景图**（推荐）：影像以视频为主、图片可选——视频需覆盖**正反两面测评**：每个地点配 1 段以上【推荐】向 + 【避雷/批判/客观对比/争议】向，标题前标注方向标签；优先景区/文旅局官方、权威媒体，其次口碑博主；每段经 B 站官方接口核验（`x/web-interface/view` 返回 code=0）并注明发布者/年份/播放量；**某地点找不到批判向素材就如实标注"未找到批判向素材"，不硬凑**；全城级警示（低价团/强制消费）单列；图片可选（如用户要求保留，按 Wikimedia Commons 核验 + 年份/许可标注 + base64 内嵌标准执行）；视频与图片均放在对应日期小节内

**输出格式**：Markdown（`.md`）为可编辑源文件；撰写完成后用本技能自带脚本 `scripts/md2html.mjs` 生成同名 `.html` 阅读版（自包含样式、浏览器直接打开、可打印/导出 PDF），与 Markdown 同目录存放。

## 4. 质量门（交付前自查）

- 每个关键事实有来源链接，或明确标注"需出发前核实"——两者必有其一
- 每个推荐地点都有缺点/坑，不因名气大回避（含争议事件）
- 体力强度分级齐全，且为受限成员（老人/低强度/幼儿）指明低强度替代方案
- 季节适配：雨季/旺季/极端天气对行程与项目的影响写清楚
- 预算分项数字自洽（勾稽关系正确，合计与分项对得上）
- 日期星期正确（用 `date` 命令核实）、首尾天定位合理（抵达休整/返程收尾）
- 输出文件保存到 `<cwd>/travel-guides/<目的地英文>-<出发日期>.md`（正文中文、文件名英文）
- Markdown 与 HTML 同目录同名；HTML 打开无乱码，表格/加粗/链接（含裸 URL）渲染完整
- 路线图只覆盖目的地内动线（不含出发地⇄机场航班段）；路段与每日行程一致（距离/车程对得上），HTML 中 ````map` 图块渲染为真实地图（总览+每日），坐标为 GCJ-02 且与高德瓦片对齐；无坐标时退化为 ````route` 示意
- 实景图/视频必须真实存在（经来源 API 或页面核验）、严格对应地点、标注年份与来源许可；找不到就如实说明，绝不使用近似图/AI 图/占位图；**视频需覆盖正反测评（【推荐】+【避雷/批判/客观/争议】）并标注方向标签**
- 每条额外/特殊要求都有落点（在行程中体现，或明确说明无法满足及替代方案）

## 5. 复用旋钮（每次旅行按此微调）

- **成员画像档位**：老人（可长走/登山受限/完全受限）、儿童（学龄前/小学/中学）、情侣、独自
- **季节档位**：雨季（上午户外、午后室内）、旺季（错峰 + 提前购票）、淡季（性价比）
- **预算档位**：经济/舒适/品质，对应住宿与餐饮区间
- **偏好开关**：深入在地 vs 打卡观光、自驾 vs 包车、度假躺平 vs 行程密集

## 6. 示例

`examples/sample-family-trip.md`：一次完整执行示例（自驾、老幼同行低强度、雨季）。文中出发地、日期与出行人构成为占位信息。撰写新攻略前先读它，校准格式、详略与"优缺点如实"的尺度。

`scripts/md2html.mjs`：Markdown → 样式化 HTML 转换脚本（node 运行、无依赖；用法 `node scripts/md2html.mjs <攻略.md>`，输出同目录同名 `.html`）。支持的图块：````map` 真实地图（总览 + 每日，Leaflet + 高德瓦片，`@overview` 首行=仅总览图）、````route` 节点示意、````video` 视频卡片（B 站嵌入播放器 + 链接兜底）；并支持 Markdown 图片（`![alt](url)`）、裸 URL 自动转链接、斜体图注；**本地相对路径图片（如 `images/x.jpg`）自动 base64 内嵌，HTML 可离线看图**。
